/* const express = require('express');
const router = express.Router();

// Modelo simulado para los tickets
let ticketNumber = 0;

// Ruta para emitir un nuevo ticket
router.post('/emitir-ticket', (req, res) => {
  ticketNumber++;
  res.json({ ticketNumber });
});

module.exports = router;
 */