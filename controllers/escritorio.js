/* const express = require('express');
const router = express.Router();

// Ruta para atender un ticket
router.post('/atender-ticket/:escritorio', (req, res) => {
  const escritorio = req.params.escritorio;
  const ticketNumber = req.body.ticketNumber;
  // Aquí tendrías la lógica para asignar el ticket al escritorio
  res.json({ message: `Ticket ${ticketNumber} asignado al escritorio ${escritorio}` });
});

module.exports = router;
 */